# 탑마케팅 회원탈퇴 기능 구현 문서

## 📅 구현 일자
2025-09-22

## 🎯 구현 목적
사용자가 언제든지 자유롭게 회원 탈퇴를 할 수 있도록 하여 개인정보 자기결정권을 보장하고,
정보통신망법 및 개인정보보호법을 준수하는 시스템 구축

## 📋 구현 범위

### Phase 1 (현재 구현)
- ✅ Soft Delete 방식의 회원 탈퇴 처리
- ✅ 개인정보 익명화 처리
- ✅ 30일 보관 후 수동 영구 삭제
- ✅ 탈퇴 사유 기록 및 로깅
- ✅ 시스템 전반의 탈퇴 회원 처리

### Phase 2 (향후 구현 예정)
- ⏳ 30일 후 자동 영구 삭제 (cron job)
- ⏳ 복구 요청 처리 시스템
- ⏳ 탈퇴 회원 통계 대시보드

## 🔧 기술적 구현 내용

### 1. 데이터베이스 스키마 변경

#### users 테이블
```sql
ALTER TABLE users
ADD COLUMN deleted_at TIMESTAMP NULL DEFAULT NULL COMMENT '탈퇴 처리 시간',
ADD COLUMN deletion_reason VARCHAR(500) NULL DEFAULT NULL COMMENT '탈퇴 사유';

-- status ENUM에 'deleted' 추가
ALTER TABLE users
MODIFY COLUMN status ENUM('active','inactive','suspended','deleted') DEFAULT 'active';
```

### 2. 백엔드 구현

#### User 모델 (/src/models/User.php)
```php
public function deleteAccount($userId, $password, $reason = null) {
    // 1. 비밀번호 검증
    // 2. 관리자 수 체크 (마지막 관리자 탈퇴 방지)
    // 3. 기업회원 활성 강의/행사 체크
    // 4. 개인정보 익명화
    // 5. status를 'deleted'로 변경
    // 6. 세션 삭제
    // 7. 활동 로그 기록
}
```

#### UserController (/src/controllers/UserController.php)
```php
public function deleteAccount() {
    // 1. 로그인 확인
    // 2. CSRF 토큰 검증
    // 3. User 모델의 deleteAccount 호출
    // 4. 성공 시 세션 파괴
}
```

#### 라우트 설정 (/src/config/routes.php)
```php
'POST:/api/user/delete-account' => ['UserController', 'deleteAccount'],
```

### 3. 프론트엔드 구현

#### 프로필 편집 페이지 (/src/views/user/edit.php)
- 회원탈퇴 버튼 추가 (취소 버튼 좌측)
- 3단계 확인 모달 시스템:
  1. 경고 및 안내 메시지
  2. 비밀번호 확인
  3. 최종 확인

### 4. 시스템 전반 탈퇴 회원 처리

#### 게시물/댓글 표시
- **Post 모델**: `CASE WHEN u.status = 'deleted' THEN '탈퇴한 회원' ELSE u.nickname END as author_name`
- **Comment 모델**: 동일한 CASE 문 적용

#### 로그인 제한
- **AuthController::login()**: 탈퇴 회원 로그인 차단
- 에러 메시지: "탈퇴한 계정입니다. 복구를 원하시면 고객센터에 문의해주세요."

#### 회원가입 제한
- **AuthController::register()**: 탈퇴한 이메일/전화번호 재사용 방지
- 기존 탈퇴 계정 체크 로직 추가

#### 비밀번호 찾기
- **AuthController::forgotPassword()**: 탈퇴 회원 제외
- 탈퇴한 회원은 비밀번호 찾기 불가

#### 강의/행사 신청자 목록
- **RegistrationController**: 탈퇴 회원 "탈퇴한 회원"으로 표시
- 신청 정보는 유지되나 개인정보는 익명화

#### 채팅 기능
- **User::getProfileImageInfo()**: 탈퇴 회원도 포함하여 조회 (메시지 표시용)
- **UserController::getProfileImage()**: 탈퇴 회원 정보 반환 시 is_deleted 플래그 추가
- **chat/index.php**: 탈퇴 회원 프로필 방문 차단, 새 채팅 시작 불가
- 탈퇴 회원은 채팅 검색에서 제외 (searchUsers는 active 사용자만 검색)

## 🔒 보안 및 개인정보 보호

### 익명화 처리 내용
- **닉네임**: `탈퇴회원_XXXXX` (랜덤 5자리)
- **이메일**: `deleted_XXXXX@deleted.com`
- **전화번호**: `010-0000-XXXX` (뒤 4자리는 ID 기반)
- **생년월일**: NULL
- **자기소개**: NULL
- **프로필 이미지**: NULL (모든 버전)
- **주소 정보**: NULL

### 데이터 보관 정책
- 탈퇴 후 30일간 복구 가능 상태로 보관
- 30일 후 수동으로 영구 삭제 가능
- 법적 의무 보관 데이터는 별도 보관

## 🧪 테스트 스크립트

### test_account_deletion.php
```bash
php /var/www/html/topmkt/test_account_deletion.php
```

테스트 항목:
1. ✅ 테스트 사용자 생성
2. ✅ 잘못된 비밀번호로 탈퇴 시도 (거부 확인)
3. ✅ 올바른 비밀번호로 탈퇴 처리
4. ✅ Status가 'deleted'로 변경 확인
5. ✅ 개인정보 익명화 확인
6. ✅ 탈퇴 로그 기록 확인

## 📝 사용자 경험 (UX)

### 탈퇴 프로세스
1. 프로필 편집 페이지에서 "회원탈퇴" 버튼 클릭
2. 경고 메시지 및 안내사항 확인
3. 비밀번호 입력으로 본인 확인
4. 최종 확인 후 탈퇴 처리
5. 자동 로그아웃 및 메인 페이지 이동

### 탈퇴 회원 표시
- 게시글/댓글 작성자: "탈퇴한 회원"
- 프로필 이미지: 기본 이미지로 대체
- 채팅: "탈퇴한 회원"으로 표시, 프로필 접근 불가

## 🚨 주의사항

1. **마지막 관리자는 탈퇴 불가**
   - 시스템에 최소 1명의 관리자 필요
   - 다른 관리자 지정 후 탈퇴 가능

2. **기업회원 탈퇴 제한**
   - 진행 중인 강의/행사가 있으면 탈퇴 불가
   - 모든 강의/행사 종료 후 탈퇴 가능

3. **데이터 영구 삭제**
   - 30일 후 수동 삭제 시 복구 불가능
   - 법적 의무 보관 데이터는 예외

## 📊 영향 받는 파일 목록

### 모델
- /src/models/User.php
- /src/models/Post.php
- /src/models/Comment.php

### 컨트롤러
- /src/controllers/UserController.php
- /src/controllers/AuthController.php
- /src/controllers/RegistrationController.php
- /src/controllers/ChatController.php

### 뷰
- /src/views/user/edit.php
- /src/views/chat/index.php
- /src/views/auth/login.php
- /src/views/auth/register.php

### 설정
- /src/config/routes.php

### 테스트
- /test_account_deletion.php

## 🔄 향후 개선 계획

1. **자동화 개선**
   - Cron job을 통한 30일 후 자동 영구 삭제
   - 탈퇴 예약 기능

2. **복구 시스템**
   - 30일 내 복구 요청 처리
   - 고객센터 연동

3. **통계 및 분석**
   - 탈퇴 사유 분석 대시보드
   - 탈퇴 방지 전략 수립

4. **법적 컴플라이언스**
   - GDPR 대응
   - 데이터 이동권 구현

## 📞 문의사항

구현 관련 문의사항은 개발팀에 연락 바랍니다.

---
**문서 작성일**: 2025-09-22
**작성자**: Claude (Anthropic)
**버전**: v1.0